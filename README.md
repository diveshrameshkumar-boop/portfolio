# Divesh R — Cinematic Portfolio

Open `index.html` with any static web server (or deploy this folder directly to Netlify, Vercel, or GitHub Pages). No build step is required.

The portfolio keeps Divesh's original professional information and resume. Motion respects the visitor's reduced-motion preference.
